<%= form_for(@operation_state) do |f| %>
  <% if @operation_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@operation_state.errors.count, "error") %> prohibited this operation_state from being saved:</h2>

      <ul>
      <% @operation_state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
